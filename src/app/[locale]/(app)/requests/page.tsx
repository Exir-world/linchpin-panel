"use client";
import RequestsList from "@/components/requests/requests";
import React from "react";

const Requests = () => {
  return (
    <div>
      <RequestsList></RequestsList>
    </div>
  );
};

export default Requests;
